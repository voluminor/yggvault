// Code generated by goconfgen; DO NOT EDIT.
// Source schema: yml/config/config.yml

package stconf

import (
	"bytes"
	"encoding/json"
	"strconv"
)

// // // // // // // // // //

func (obj *ConfigObj) RenderJSON(partialFlag bool) ([]byte, error) {
	var bufferObj bytes.Buffer
	includeFlag, err := appendJSONConfigObj(&bufferObj, obj, partialFlag, "", true)
	if err != nil {
		return nil, err
	}
	if !includeFlag {
		return []byte("{}\n"), nil
	}
	return bufferObj.Bytes(), nil
}

func appendJSONConfigObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONInfoObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "info")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("release_mirrors") {
			valueObj, err := renderValueReleaseMirrors(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "release_mirrors")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "web")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONYggObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "ygg")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONLoggingObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "logging")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONMetricsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "metrics")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRateLimitObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "rate_limit")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONSourceObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "source")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRescanObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "rescan")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONStorageObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "storage")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONCacheObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "cache")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONOverlayObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "overlay")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONHistoryPolicyObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "history_policy")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONUpstreamAvailabilityObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "upstream_availability")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONBrotherObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "brother")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("shutdown_timeout") {
			valueObj, err := renderValueShutdownTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "shutdown_timeout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONProfilingObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "profiling")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONInfoObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("info.name") {
			valueObj, err := renderValueInfoName(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "name")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("info.description") {
			valueObj, err := renderValueInfoDescription(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "description")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("info.location") {
			valueObj, err := renderValueInfoLocation(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "location")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("info.contacts") {
			valueObj, err := renderValueInfoContacts(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "contacts")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebServerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "server")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebIngressObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "ingress")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebStaticObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "static")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebRoutingObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "routing")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebPagesObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "pages")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebServerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.server.domain") {
			valueObj, err := renderValueWebServerDomain(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "domain")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.server.mode") {
			valueObj, err := renderValueWebServerMode(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "mode")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebServerTlsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "tls")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebServerSharedObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "shared")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebServerSplitObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "split")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebServerSingleObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "single")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebServerTlsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.server.tls.cert") {
			valueObj, err := renderValueWebServerTlsCert(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "cert")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.server.tls.key") {
			valueObj, err := renderValueWebServerTlsKey(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "key")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebServerSharedObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.server.shared.listen") {
			valueObj, err := renderValueWebServerSharedListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "listen")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebServerSplitObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebServerSplitHttpObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "http")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONWebServerSplitHttpsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "https")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebServerSplitHttpObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.server.split.http.listen") {
			valueObj, err := renderValueWebServerSplitHttpListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "listen")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebServerSplitHttpsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.server.split.https.listen") {
			valueObj, err := renderValueWebServerSplitHttpsListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "listen")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebServerSingleObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.server.single.proto") {
			valueObj, err := renderValueWebServerSingleProto(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "proto")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.server.single.listen") {
			valueObj, err := renderValueWebServerSingleListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "listen")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebIngressObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.ingress.max_request_uri_bytes") {
			valueObj, err := renderValueWebIngressMaxRequestUriBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_request_uri_bytes")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.ingress.read_buffer_size") {
			valueObj, err := renderValueWebIngressReadBufferSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "read_buffer_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.ingress.read_header_timeout") {
			valueObj, err := renderValueWebIngressReadHeaderTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "read_header_timeout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.ingress.idle_timeout") {
			valueObj, err := renderValueWebIngressIdleTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "idle_timeout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebStaticObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.static.dir") {
			valueObj, err := renderValueWebStaticDir(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "dir")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.static.index_file") {
			valueObj, err := renderValueWebStaticIndexFile(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "index_file")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.static.max_size") {
			valueObj, err := renderValueWebStaticMaxSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.static.cache_max_age") {
			valueObj, err := renderValueWebStaticCacheMaxAge(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "cache_max_age")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.static.deny") {
			valueObj, err := renderValueWebStaticDeny(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "deny")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebRoutingObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.routing.prefix") {
			valueObj, err := renderValueWebRoutingPrefix(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "prefix")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONWebPagesObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("web.pages.list_page_size") {
			valueObj, err := renderValueWebPagesListPageSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "list_page_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.pages.feed_size") {
			valueObj, err := renderValueWebPagesFeedSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "feed_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.pages.sitemap_size") {
			valueObj, err := renderValueWebPagesSitemapSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "sitemap_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONYggObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("ygg.pem_key") {
			valueObj, err := renderValueYggPemKey(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "pem_key")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONYggPeersObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "peers")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONYggPeersObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("ygg.peers.initial") {
			valueObj, err := renderValueYggPeersInitial(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "initial")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.max_per_proto") {
			valueObj, err := renderValueYggPeersMaxPerProto(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_per_proto")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.probe_timeout") {
			valueObj, err := renderValueYggPeersProbeTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "probe_timeout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.refresh_interval") {
			valueObj, err := renderValueYggPeersRefreshInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "refresh_interval")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.batch_size") {
			valueObj, err := renderValueYggPeersBatchSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "batch_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONLoggingObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONLoggingConsoleObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "console")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONLoggingFileObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "file")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONLoggingVictorialogsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "victorialogs")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONLoggingConsoleObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("logging.console.enabled") {
			valueObj, err := renderValueLoggingConsoleEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "enabled")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.console.level") {
			valueObj, err := renderValueLoggingConsoleLevel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "level")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONLoggingFileObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("logging.file.enabled") {
			valueObj, err := renderValueLoggingFileEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "enabled")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.level") {
			valueObj, err := renderValueLoggingFileLevel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "level")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.dir") {
			valueObj, err := renderValueLoggingFileDir(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "dir")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.max_size") {
			valueObj, err := renderValueLoggingFileMaxSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.max_backups") {
			valueObj, err := renderValueLoggingFileMaxBackups(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_backups")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.max_age") {
			valueObj, err := renderValueLoggingFileMaxAge(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_age")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.compress") {
			valueObj, err := renderValueLoggingFileCompress(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "compress")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONLoggingVictorialogsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.enabled") {
			valueObj, err := renderValueLoggingVictorialogsEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "enabled")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.url") {
			valueObj, err := renderValueLoggingVictorialogsUrl(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "url")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.level") {
			valueObj, err := renderValueLoggingVictorialogsLevel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "level")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.flush_interval") {
			valueObj, err := renderValueLoggingVictorialogsFlushInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "flush_interval")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.batch_max_bytes") {
			valueObj, err := renderValueLoggingVictorialogsBatchMaxBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "batch_max_bytes")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.buffer_max_lines") {
			valueObj, err := renderValueLoggingVictorialogsBufferMaxLines(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "buffer_max_lines")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.timeout") {
			valueObj, err := renderValueLoggingVictorialogsTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "timeout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.gzip") {
			valueObj, err := renderValueLoggingVictorialogsGzip(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "gzip")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONMetricsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONMetricsWebObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "web")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONMetricsYggObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "ygg")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONMetricsErrorsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "errors")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.snapshot_interval") {
			valueObj, err := renderValueMetricsSnapshotInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "snapshot_interval")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONMetricsPushObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "push")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONMetricsWebObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("metrics.web.public") {
			valueObj, err := renderValueMetricsWebPublic(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "public")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.web.internal") {
			valueObj, err := renderValueMetricsWebInternal(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "internal")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONMetricsYggObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("metrics.ygg.public") {
			valueObj, err := renderValueMetricsYggPublic(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "public")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.ygg.internal") {
			valueObj, err := renderValueMetricsYggInternal(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "internal")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONMetricsErrorsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("metrics.errors.recent_buffer") {
			valueObj, err := renderValueMetricsErrorsRecentBuffer(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "recent_buffer")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONMetricsPushObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("metrics.push.enabled") {
			valueObj, err := renderValueMetricsPushEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "enabled")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.push.url") {
			valueObj, err := renderValueMetricsPushUrl(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "url")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.push.interval") {
			valueObj, err := renderValueMetricsPushInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "interval")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.push.timeout") {
			valueObj, err := renderValueMetricsPushTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "timeout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRateLimitObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRateLimitWebObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "web")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRateLimitYggObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "ygg")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRateLimitWebObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRateLimitWebHttpObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "http")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRateLimitWebHttpsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "https")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRateLimitWebHttpObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.requests_per_second") {
			valueObj, err := renderValueRateLimitWebHttpRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "requests_per_second")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.burst") {
			valueObj, err := renderValueRateLimitWebHttpBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "burst")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRateLimitWebHttpPerPeerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "per_peer")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRateLimitWebHttpPerPeerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.per_peer.requests_per_second") {
			valueObj, err := renderValueRateLimitWebHttpPerPeerRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "requests_per_second")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.per_peer.burst") {
			valueObj, err := renderValueRateLimitWebHttpPerPeerBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "burst")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.per_peer.max_tracked") {
			valueObj, err := renderValueRateLimitWebHttpPerPeerMaxTracked(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_tracked")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRateLimitWebHttpsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.requests_per_second") {
			valueObj, err := renderValueRateLimitWebHttpsRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "requests_per_second")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.burst") {
			valueObj, err := renderValueRateLimitWebHttpsBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "burst")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRateLimitWebHttpsPerPeerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "per_peer")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRateLimitWebHttpsPerPeerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.per_peer.requests_per_second") {
			valueObj, err := renderValueRateLimitWebHttpsPerPeerRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "requests_per_second")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.per_peer.burst") {
			valueObj, err := renderValueRateLimitWebHttpsPerPeerBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "burst")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.per_peer.max_tracked") {
			valueObj, err := renderValueRateLimitWebHttpsPerPeerMaxTracked(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_tracked")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRateLimitYggObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.requests_per_second") {
			valueObj, err := renderValueRateLimitYggRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "requests_per_second")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.burst") {
			valueObj, err := renderValueRateLimitYggBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "burst")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRateLimitYggPerPeerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "per_peer")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRateLimitYggPerPeerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.per_peer.requests_per_second") {
			valueObj, err := renderValueRateLimitYggPerPeerRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "requests_per_second")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.per_peer.burst") {
			valueObj, err := renderValueRateLimitYggPerPeerBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "burst")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.per_peer.max_tracked") {
			valueObj, err := renderValueRateLimitYggPerPeerMaxTracked(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_tracked")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONSourceObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("source.request_timeout") {
			valueObj, err := renderValueSourceRequestTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "request_timeout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.download_max_parallel") {
			valueObj, err := renderValueSourceDownloadMaxParallel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "download_max_parallel")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONSourceRetryObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "retry")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONSourceCredentialsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "credentials")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONSourceRetryObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("source.retry.backoff_initial") {
			valueObj, err := renderValueSourceRetryBackoffInitial(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "backoff_initial")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.retry.backoff_max") {
			valueObj, err := renderValueSourceRetryBackoffMax(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "backoff_max")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.retry.jitter_percent") {
			valueObj, err := renderValueSourceRetryJitterPercent(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "jitter_percent")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONSourceCredentialsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("source.credentials.github") {
			valueObj, err := renderValueSourceCredentialsGithub(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "github")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.credentials.gitlab") {
			valueObj, err := renderValueSourceCredentialsGitlab(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "gitlab")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.credentials.bitbucket") {
			valueObj, err := renderValueSourceCredentialsBitbucket(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "bitbucket")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.credentials.others") {
			valueObj, err := renderValueSourceCredentialsOthers(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "others")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRescanObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("rescan.interval") {
			valueObj, err := renderValueRescanInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "interval")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rescan.max_parallel_keys") {
			valueObj, err := renderValueRescanMaxParallelKeys(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_parallel_keys")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rescan.initial_depth") {
			valueObj, err := renderValueRescanInitialDepth(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "initial_depth")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONRescanVerifyObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "verify")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONRescanVerifyObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("rescan.verify.recent_interval") {
			valueObj, err := renderValueRescanVerifyRecentInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "recent_interval")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rescan.verify.archive_interval") {
			valueObj, err := renderValueRescanVerifyArchiveInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "archive_interval")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONStorageObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("storage.dir") {
			valueObj, err := renderValueStorageDir(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "dir")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.in_flight_read_bytes") {
			valueObj, err := renderValueStorageInFlightReadBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "in_flight_read_bytes")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.overlay_build_max_parallel") {
			valueObj, err := renderValueStorageOverlayBuildMaxParallel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "overlay_build_max_parallel")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONStoragePebbleObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "pebble")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONStorageQuotaObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "quota")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONStorageHotObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "hot")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONStorageArchiveLimitsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "archive_limits")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONStoragePebbleObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("storage.pebble.compression") {
			valueObj, err := renderValueStoragePebbleCompression(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "compression")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.pebble.block_cache_size") {
			valueObj, err := renderValueStoragePebbleBlockCacheSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "block_cache_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.pebble.memtable_size") {
			valueObj, err := renderValueStoragePebbleMemtableSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "memtable_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.pebble.verify_on_read") {
			valueObj, err := renderValueStoragePebbleVerifyOnRead(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "verify_on_read")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONStorageQuotaObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("storage.quota.max_total_size") {
			valueObj, err := renderValueStorageQuotaMaxTotalSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_total_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.quota.evict_to_size") {
			valueObj, err := renderValueStorageQuotaEvictToSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "evict_to_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.quota.retain_latest_per_key") {
			valueObj, err := renderValueStorageQuotaRetainLatestPerKey(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "retain_latest_per_key")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.quota.max_versions_per_key") {
			valueObj, err := renderValueStorageQuotaMaxVersionsPerKey(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_versions_per_key")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.quota.gc_interval") {
			valueObj, err := renderValueStorageQuotaGcInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "gc_interval")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONStorageHotObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("storage.hot.max_size") {
			valueObj, err := renderValueStorageHotMaxSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.hot.idle_ttl") {
			valueObj, err := renderValueStorageHotIdleTtl(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "idle_ttl")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.hot.retain") {
			valueObj, err := renderValueStorageHotRetain(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "retain")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.hot.verify_on_read") {
			valueObj, err := renderValueStorageHotVerifyOnRead(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "verify_on_read")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONStorageArchiveLimitsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONStorageArchiveLimitsSizeObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "size")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONStorageArchiveLimitsEntriesObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "entries")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONStorageArchiveLimitsSizeObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.size.compressed") {
			valueObj, err := renderValueStorageArchiveLimitsSizeCompressed(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "compressed")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.size.unpacked") {
			valueObj, err := renderValueStorageArchiveLimitsSizeUnpacked(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "unpacked")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.size.per_file") {
			valueObj, err := renderValueStorageArchiveLimitsSizePerFile(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "per_file")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONStorageArchiveLimitsEntriesObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.entries.count") {
			valueObj, err := renderValueStorageArchiveLimitsEntriesCount(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "count")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.entries.path_bytes") {
			valueObj, err := renderValueStorageArchiveLimitsEntriesPathBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "path_bytes")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONCacheObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("cache.metadata_max_size") {
			valueObj, err := renderValueCacheMetadataMaxSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "metadata_max_size")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONOverlayObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONOverlayGoObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "go")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONOverlayGoObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("overlay.go.rewrite_enabled") {
			valueObj, err := renderValueOverlayGoRewriteEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "rewrite_enabled")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONHistoryPolicyObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("history_policy.mutation") {
			valueObj, err := renderValueHistoryPolicyMutation(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "mutation")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONHistoryPolicyDeletionObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "deletion")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("history_policy.prefix") {
			valueObj, err := renderValueHistoryPolicyPrefix(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "prefix")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("history_policy.max_events") {
			valueObj, err := renderValueHistoryPolicyMaxEvents(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_events")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONHistoryPolicyDeletionObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("history_policy.deletion.mode") {
			valueObj, err := renderValueHistoryPolicyDeletionMode(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "mode")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("history_policy.deletion.grace_cycles") {
			valueObj, err := renderValueHistoryPolicyDeletionGraceCycles(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "grace_cycles")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONUpstreamAvailabilityObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("upstream_availability.permanent_after_cycles") {
			valueObj, err := renderValueUpstreamAvailabilityPermanentAfterCycles(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "permanent_after_cycles")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONBrotherObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("brother.first_source_timeout") {
			valueObj, err := renderValueBrotherFirstSourceTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "first_source_timeout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONBrotherRpcObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "rpc")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.prefer") {
			valueObj, err := renderValueBrotherPrefer(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "prefer")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.transport_order") {
			valueObj, err := renderValueBrotherTransportOrder(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "transport_order")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONBrotherRpcObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("brother.rpc.web_enabled") {
			valueObj, err := renderValueBrotherRpcWebEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "web_enabled")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.ygg_enabled") {
			valueObj, err := renderValueBrotherRpcYggEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "ygg_enabled")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.max_parallel") {
			valueObj, err := renderValueBrotherRpcMaxParallel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_parallel")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.max_parallel_per_peer") {
			valueObj, err := renderValueBrotherRpcMaxParallelPerPeer(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_parallel_per_peer")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.rate_per_sec") {
			valueObj, err := renderValueBrotherRpcRatePerSec(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "rate_per_sec")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.max_fetch_response_bytes") {
			valueObj, err := renderValueBrotherRpcMaxFetchResponseBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_fetch_response_bytes")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.max_fetch_batch_count") {
			valueObj, err := renderValueBrotherRpcMaxFetchBatchCount(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_fetch_batch_count")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONProfilingObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("profiling.enabled") {
			valueObj, err := renderValueProfilingEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "enabled")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("profiling.listen") {
			valueObj, err := renderValueProfilingListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "listen")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("profiling.max_profile_seconds") {
			valueObj, err := renderValueProfilingMaxProfileSeconds(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "max_profile_seconds")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONProfilingContentionObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "contention")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONProfilingContentionObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("profiling.contention.block_rate") {
			valueObj, err := renderValueProfilingContentionBlockRate(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "block_rate")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("profiling.contention.mutex_fraction") {
			valueObj, err := renderValueProfilingContentionMutexFraction(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "mutex_fraction")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONEntryPrefix(bufferObj *bytes.Buffer, firstFlag *bool, indentText string, keyText string) {
	if !*firstFlag {
		bufferObj.WriteString(",\n")
	}
	*firstFlag = false
	bufferObj.WriteString(indentText)
	bufferObj.WriteString("  ")
	bufferObj.WriteString(strconv.Quote(keyText))
	bufferObj.WriteString(": ")
}
