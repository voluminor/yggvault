// Code generated by goconfgen; DO NOT EDIT.
// Source schema: yml/config/config.yml

package stconf

import (
	"bytes"
	"encoding/json"
	"strings"

	"github.com/hjson/hjson-go/v4"
)

// // // // // // // // // //

func (obj *ConfigObj) RenderHJSON(partialFlag bool) ([]byte, error) {
	var bufferObj bytes.Buffer
	includeFlag, err := appendHJSONConfigObj(&bufferObj, obj, partialFlag, "", true)
	if err != nil {
		return nil, err
	}
	if !includeFlag {
		return []byte("{}\n"), nil
	}
	return bufferObj.Bytes(), nil
}

func appendHJSONConfigObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONInfoObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "info", "Public identity card for this node. It is returned by HTTP /info and published in Yggdrasil NodeInfo through the info and inet sigils.\nDo not put secrets here: every mesh peer can read NodeInfo, and every HTTP client can read /info.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("release_mirrors") {
			valueObj, err := renderValueReleaseMirrors(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "release_mirrors", "Mirror source registry: local key -> upstream repository root URL.\nKeys must match `^[a-z0-9][a-z0-9._-]{1,38}[a-z0-9]$`, must not equal web.server.domain, and must not use reserved route names or Go major suffixes such as `v2`.\nValues must be non-empty HTTP or HTTPS URLs with a host.\nA URL must not point at this node itself: hosts equal to web.server.domain or to this node's own Yggdrasil host are rejected, because a node cannot mirror itself.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "web", "Web ingress, route topology, static file serving, and browser/API pagination.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONYggObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "ygg", "Yggdrasil ingress and peer manager settings.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONLoggingObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "logging", "Runtime logging pipeline and event sinks.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONMetricsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "metrics", "Metrics publication switches. If open, internal, and push are all disabled, telemetry is not built and metrics are not collected.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRateLimitObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "rate_limit", "Token-bucket request limiting per transport.\nEach transport has a global bucket. requests_per_second=0 disables that bucket.\nEach transport can also have a per_peer bucket keyed by client address. per_peer.requests_per_second=0 disables per-peer limiting while leaving the global bucket independent.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONSourceObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "source", "Outgoing source access: upstream timeouts, download parallelism, retries, and provider credentials.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRescanObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "rescan", "Periodic source refresh scheduler.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONStorageObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "storage", "Durable source-of-truth storage and regenerable hot artifact cache under one root directory.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONCacheObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "cache", "RAM cache for small generated payloads only: list/latest/info/mod/composer metadata and ETags. Artifact bodies are served from hot files on disk, not from this cache.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONOverlayObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "overlay", "Safety settings for ecosystem-specific overlay materialization.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONHistoryPolicyObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "history_policy", "Version mutation and deletion policy for the local history chain.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONUpstreamAvailabilityObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "upstream_availability", "Policy for classifying upstream availability.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONBrotherObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "brother", "RPC synchronization between yggvault instances. CONNECT /rpc can be exposed on the web and Yggdrasil listeners independently.\nThe channel is read-only. When it is exposed on web, treat it as a public mirror surface and keep the RPC limits bounded.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("shutdown_timeout") {
			valueObj, err := renderValueShutdownTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "shutdown_timeout", "Global graceful shutdown budget. Storage close still gets its own integrity budget in runtime shutdown code.\nAlso applied as the embedded Yggdrasil node close budget: node teardown that exceeds it continues in the background.\nAccepts values such as `10s`, `1m`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONProfilingObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "profiling", "Built-in net/http/pprof and runtime-trace listener for load diagnostics. Disabled mode starts no listener. Enabled mode must bind only to loopback; use an SSH tunnel for remote access.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONInfoObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("info.name") {
			valueObj, err := renderValueInfoName(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "name", "Public node name used by /info and the Yggdrasil info sigil.\nAllowed length is 4..64 characters. Use lowercase letters, digits, dot, underscore, and hyphen only.\nEmpty value is allowed. Runtime derives a valid name from web.server.domain, then from the Yggdrasil host, then falls back to the binary name.\nExamples: `mirror.example.org`, `vault.example`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("info.description") {
			valueObj, err := renderValueInfoDescription(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "description", "Free-form public description or peering policy used by /info and the Yggdrasil info sigil.\nNon-empty values must be 2..514 characters and must not have leading or trailing whitespace.\nExample: `Go and Composer release mirror; peering on request`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("info.location") {
			valueObj, err := renderValueInfoLocation(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "location", "Optional public location text used by /info and the Yggdrasil info sigil.\nExample: `EU/Frankfurt`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("info.contacts") {
			valueObj, err := renderValueInfoContacts(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "contacts", "Public contact groups used by /info and the Yggdrasil info sigil.\nGroup keys use `[a-z0-9.-]`, length 2..32. Values use length 3..258 and must not have leading or trailing whitespace.\nAt most 8 groups and at most 8 values per group.\nExample: `{mail: ['abuse@example.org'], admin: ['https://example.org/contact']}`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebServerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "server", "Web listener publication mode and bind addresses.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebIngressObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "ingress", "Request-line, header, and connection time limits applied to all HTTP listeners.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebStaticObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "static", "Optional user static tree served by the web listener.\nWhen disabled, API and browser routes live at the root. When enabled, user static files own `/`, while per-key API/browser routes move under web.routing.prefix. Stable service routes such as /health, /info, /metrics, and /openapi.json stay at their well-known root paths.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebRoutingObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "routing", "API prefix used only when web.static.dir enables nested routing.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebPagesObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "pages", "Rendering and listing limits for browser/API pages.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebServerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.server.domain") {
			valueObj, err := renderValueWebServerDomain(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "domain", "Canonical public web host.\nUsed for entry host context, Go module paths, generated links, install commands, and the inet sigil when the domain is public.\nRequired when any web listener is enabled.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.server.mode") {
			valueObj, err := renderValueWebServerMode(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "mode", "Listener topology.\n`shared`: one listener behind a TLS-terminating reverse proxy. Generated external scheme is always https.\n`split`: separate HTTP and HTTPS listeners. Both listen addresses must be set.\n`single`: one listener with an explicit protocol from web.server.single.proto.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebServerTlsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "tls", "TLS termination performed by this process.\nBoth cert and key empty means the listener is plain TCP and TLS is expected to terminate upstream, usually in a reverse proxy.\nSetting exactly one of cert/key is a startup error.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebServerSharedObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "shared", "Single listener for the `shared` mode. Intended for a reverse proxy that terminates TLS before yggvault.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebServerSplitObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "split", "Separate listeners for HTTP and HTTPS in `split` mode.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebServerSingleObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "single", "One listener with a fixed advertised protocol.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebServerTlsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.server.tls.cert") {
			valueObj, err := renderValueWebServerTlsCert(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "cert", "Path to the TLS certificate file. Must be set together with web.server.tls.key.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.server.tls.key") {
			valueObj, err := renderValueWebServerTlsKey(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "key", "Path to the TLS private key file. Must be set together with web.server.tls.cert.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebServerSharedObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.server.shared.listen") {
			valueObj, err := renderValueWebServerSharedListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "listen", "Bind address for the shared listener. Empty disables web ingress when mode=shared.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebServerSplitObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebServerSplitHttpObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "http", "Plain HTTP listener.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONWebServerSplitHttpsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "https", "HTTPS listener.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebServerSplitHttpObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.server.split.http.listen") {
			valueObj, err := renderValueWebServerSplitHttpListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "listen", "Bind address for HTTP. Required when mode=split.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebServerSplitHttpsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.server.split.https.listen") {
			valueObj, err := renderValueWebServerSplitHttpsListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "listen", "Bind address for HTTPS. Required when mode=split.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebServerSingleObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.server.single.proto") {
			valueObj, err := renderValueWebServerSingleProto(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "proto", "External scheme for single-listener mode.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.server.single.listen") {
			valueObj, err := renderValueWebServerSingleListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "listen", "Bind address for the single listener. Empty disables web ingress when mode=single.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebIngressObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.ingress.max_request_uri_bytes") {
			valueObj, err := renderValueWebIngressMaxRequestUriBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_request_uri_bytes", "Maximum raw RequestURI length, including path and query. Valid range: 256..16384 bytes.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.ingress.read_buffer_size") {
			valueObj, err := renderValueWebIngressReadBufferSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "read_buffer_size", "Read buffer for request line and headers. Must be at least max_request_uri_bytes + 1024. Accepts bytes or size strings such as `8KiB` and `256KiB`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.ingress.read_header_timeout") {
			valueObj, err := renderValueWebIngressReadHeaderTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "read_header_timeout", "Timeout for reading incoming request headers on every listener. This is the slow-loris guard. Accepts Go duration strings such as `10s`, `5s`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.ingress.idle_timeout") {
			valueObj, err := renderValueWebIngressIdleTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "idle_timeout", "Keep-alive idle timeout for incoming connections. Limits how long slow or idle clients can hold sockets. Accepts values such as `120s`, `1m`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.ingress.max_connections") {
			valueObj, err := renderValueWebIngressMaxConnections(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_connections", "Maximum number of simultaneously accepted connections per listener, applied to every HTTP listener including web and Yggdrasil. 0 disables the limit. When set, connections beyond the limit wait in the accept backlog until a slot frees.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebStaticObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.static.dir") {
			valueObj, err := renderValueWebStaticDir(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "dir", "Root directory for static files.\nEmpty value disables static serving and keeps root API routing.\nNon-empty value enables nested routing. The path must exist, be a readable directory, and contain web.static.index_file.\nMust not overlap storage.dir or logging.file.dir.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.static.index_file") {
			valueObj, err := renderValueWebStaticIndexFile(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "index_file", "Static index file returned for `/`. Must be a clean relative path inside web.static.dir.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.static.max_size") {
			valueObj, err := renderValueWebStaticMaxSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_size", "Maximum total size of regular files admitted into the static tree. File bodies are served from disk; the snapshot keeps path metadata, ETags, and routing data. Accepts values such as `256mb`, `1GiB`, or bytes.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.static.cache_max_age") {
			valueObj, err := renderValueWebStaticCacheMaxAge(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "cache_max_age", "Client cache lifetime for static files through Cache-Control max-age. Accepts values such as `24h`, `15m`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.static.deny") {
			valueObj, err := renderValueWebStaticDeny(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "deny", "Case-insensitive deny rules for static files.\nExtension rule example: `.pem`.\nPath rule examples: `/private/`, `/private/secrets.txt`. Paths are web paths relative to the static root.\nRules must not be empty and must not contain `..` or backslash.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebRoutingObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.routing.prefix") {
			valueObj, err := renderValueWebRoutingPrefix(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "prefix", "Single path segment used before per-key API and browser routes in nested mode.\nMust match `^[a-z0-9][a-z0-9._-]{0,31}$`.\nMust not be a reserved service or generated-asset path: `health`, `info`, `metrics`, `openapi.json`, `og`, `logo`, `og.png`, `favicon.ico`, `sitemap.xml`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONWebPagesObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("web.pages.list_page_size") {
			valueObj, err := renderValueWebPagesListPageSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "list_page_size", "Page size for releases.json and the HTML key page. Valid range: 1..5000.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.pages.feed_size") {
			valueObj, err := renderValueWebPagesFeedSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "feed_size", "Number of recent history events exposed in Atom feeds. Includes publish and archive mutation events. Valid range: 1..500.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("web.pages.sitemap_size") {
			valueObj, err := renderValueWebPagesSitemapSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "sitemap_size", "Maximum number of URLs listed in /sitemap.xml (catalog, keys, and versions). On overflow, newest versions win. Valid range: 1..50000.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONYggObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("ygg.pem_key") {
			valueObj, err := renderValueYggPemKey(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "pem_key", "Path to the node private key in PEM format. Create a key with `yggvault --make-ygg-key`.\nRelative paths are resolved against the process working directory.\nEmpty value fully disables Yggdrasil ingress.\nWhen enabled, the node publishes HTTP on fixed Yggdrasil service port 80. Its host is `<PublicKey>.pk.ygg`; its transport address is an IPv6 address in 200::/7.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONYggPeersObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "peers", "Yggdrasil peer manager configuration. Empty initial peer list disables peer manager startup.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONYggPeersObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("ygg.peers.initial") {
			valueObj, err := renderValueYggPeersInitial(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "initial", "Initial peer URI candidates. Empty list disables peer manager startup.\nInvalid or duplicate entries are dropped with startup warnings; validation fails only when no usable candidate remains.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.passive") {
			valueObj, err := renderValueYggPeersPassive(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "passive", "Keeps every configured peer connected and managed, skipping latency-based selection.\nWhen enabled, ygg.peers.max_per_proto and ygg.peers.min_peers are ignored and health recovery does not run; scheduled ygg.peers.refresh_interval refreshes still reconnect lost peers.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.max_per_proto") {
			valueObj, err := renderValueYggPeersMaxPerProto(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_per_proto", "Target number of best peers retained per transport protocol.\nIgnored when ygg.peers.passive is enabled.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.probe_timeout") {
			valueObj, err := renderValueYggPeersProbeTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "probe_timeout", "Timeout for one peer probe batch. Accepts values such as `5s`, `500ms`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.refresh_interval") {
			valueObj, err := renderValueYggPeersRefreshInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "refresh_interval", "Interval between scheduled peer-pool refreshes. Zero disables scheduled refreshes after the startup selection; outage and min_peers recovery still run independently through ygg.peers.health_interval.\nOne refresh re-probes the whole candidate pool, so non-zero values below 1m are raised to 1m at startup with a warning. Accepts values such as `5m`, `1h`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.batch_size") {
			valueObj, err := renderValueYggPeersBatchSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "batch_size", "Maximum new peer connections attempted in one refresh cycle. Values above 256 are capped by the library internally.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.min_peers") {
			valueObj, err := renderValueYggPeersMinPeers(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "min_peers", "Early-recovery threshold: when the count of active peers stays at or below this value for ygg.peers.min_peers_confirmations consecutive health checks, an unscheduled peer-pool refresh runs.\n0 disables early recovery. Valid range: 0..256.\nMust stay below the selectable peer capacity: the sum over distinct URI schemes in ygg.peers.initial of min(scheme candidates, max_per_proto).\nIgnored in passive mode and when health recovery is disabled (ygg.peers.health_interval=0).")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.min_peers_confirmations") {
			valueObj, err := renderValueYggPeersMinPeersConfirmations(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "min_peers_confirmations", "Number of consecutive low-peer-count health checks required before early recovery triggers. Guards against refresh storms on short connectivity blips. Valid range: 1..1000.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.health_interval") {
			valueObj, err := renderValueYggPeersHealthInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "health_interval", "Interval between peer health checks driving outage detection and ygg.peers.min_peers early recovery.\n0 disables health recovery. Non-zero values must be at least 1s. Accepts values such as `10s`, `1m`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("ygg.peers.reprobe_interval") {
			valueObj, err := renderValueYggPeersReprobeInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "reprobe_interval", "Minimum holdoff before reconnecting a reachable peer that lost latency selection. Prevents flapping between near-equal candidates.\n0 disables the holdoff. Accepts values such as `30m`, `1h`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONLoggingObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONLoggingConsoleObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "console", "stdout/stderr logging sink.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONLoggingFileObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "file", "Rotating file logging sink.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONLoggingVictorialogsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "victorialogs", "Best-effort HTTP push of JSON log lines to VictoriaLogs. Intended for a trusted network; no auth is configured here, and traffic uses regular networking, not Yggdrasil.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONLoggingConsoleObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("logging.console.enabled") {
			valueObj, err := renderValueLoggingConsoleEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "enabled", "Enables console logging. At least one logging sink must stay enabled.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.console.level") {
			valueObj, err := renderValueLoggingConsoleLevel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "level", "Minimum severity for console events. Severity order is trace < debug < info < warn < error < fatal < panic.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONLoggingFileObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("logging.file.enabled") {
			valueObj, err := renderValueLoggingFileEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "enabled", "Enables file logging. Requires logging.file.dir.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.level") {
			valueObj, err := renderValueLoggingFileLevel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "level", "Minimum severity for file events. Same level names and ordering as logging.console.level.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.dir") {
			valueObj, err := renderValueLoggingFileDir(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "dir", "Directory for rotated log files. Runtime writes `<target.Name>.log` inside this directory. Must not overlap storage.dir or web.static.dir.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.max_size") {
			valueObj, err := renderValueLoggingFileMaxSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_size", "Rotation threshold for one log file. Accepts values such as `100mb`, `512MiB`, or bytes.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.max_backups") {
			valueObj, err := renderValueLoggingFileMaxBackups(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_backups", "Maximum number of retained rotated files.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.max_age") {
			valueObj, err := renderValueLoggingFileMaxAge(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_age", "Retention window for rotated files. Accepts values such as `24h`, `336h`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.file.compress") {
			valueObj, err := renderValueLoggingFileCompress(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "compress", "Compresses rotated log files.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONLoggingVictorialogsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.enabled") {
			valueObj, err := renderValueLoggingVictorialogsEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "enabled", "Enables the VictoriaLogs sink. It can be the only enabled logging sink.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.url") {
			valueObj, err := renderValueLoggingVictorialogsUrl(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "url", "Base VictoriaLogs URL, for example `http://127.0.0.1:9428`. Runtime appends `/insert/jsonline`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.level") {
			valueObj, err := renderValueLoggingVictorialogsLevel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "level", "Minimum severity sent to VictoriaLogs. Same level names and ordering as logging.console.level.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.flush_interval") {
			valueObj, err := renderValueLoggingVictorialogsFlushInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "flush_interval", "Maximum delay before flushing the current batch. Accepts values such as `5s`, `1s`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.batch_max_bytes") {
			valueObj, err := renderValueLoggingVictorialogsBatchMaxBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "batch_max_bytes", "Batch size threshold for early flush. Must be between 1 byte and 64 MB. Accepts values such as `1mb`, `512KiB`, or bytes.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.buffer_max_lines") {
			valueObj, err := renderValueLoggingVictorialogsBufferMaxLines(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "buffer_max_lines", "Maximum queued log lines. On overflow, new lines are dropped and counted; the file sink remains the source of truth if enabled. Hard cap: 1,000,000 lines.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.timeout") {
			valueObj, err := renderValueLoggingVictorialogsTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "timeout", "Timeout for one VictoriaLogs push request. Accepts values such as `10s`, `5s`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.victorialogs.gzip") {
			valueObj, err := renderValueLoggingVictorialogsGzip(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "gzip", "Compresses VictoriaLogs request bodies with gzip.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONMetricsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONMetricsWebObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "web", "Metrics exposed on the web ingress.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONMetricsYggObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "ygg", "Metrics exposed on the Yggdrasil ingress.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONMetricsErrorsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "errors", "Metrics derived from the in-memory Error Registry.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.snapshot_interval") {
			valueObj, err := renderValueMetricsSnapshotInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "snapshot_interval", "Interval for rebuilding the pre-rendered metrics snapshot. Accepts values such as `1s`, `5s`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONMetricsPushObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "push", "Best-effort HTTP push of the metrics snapshot to VictoriaMetrics. Intended for a trusted network; no auth is configured here, and traffic uses regular networking, not Yggdrasil.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONMetricsWebObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("metrics.web.public") {
			valueObj, err := renderValueMetricsWebPublic(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "public", "Publishes the HTML metrics index at /metrics and the JSON metric groups on web routes: /metrics/core, /metrics/cache, /metrics/errors, /metrics/rescan, and /metrics/ygg. The ygg group exists only while Yggdrasil ingress is running and returns 404 otherwise. Disabled by default: metrics stay operator-only until explicitly exposed.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.web.internal") {
			valueObj, err := renderValueMetricsWebInternal(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "internal", "Publishes the full Prometheus text (0.0.4) exposition at /metrics/internal on web routes, for aggregators such as VictoriaMetrics or Prometheus.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONMetricsYggObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("metrics.ygg.public") {
			valueObj, err := renderValueMetricsYggPublic(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "public", "Publishes the HTML metrics index and JSON metric groups on Yggdrasil routes. Same group set as metrics.web.public. Disabled by default.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.ygg.internal") {
			valueObj, err := renderValueMetricsYggInternal(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "internal", "Publishes the full Prometheus text (0.0.4) exposition at /metrics/internal on Yggdrasil routes, for internal aggregators.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONMetricsErrorsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("metrics.errors.recent_buffer") {
			valueObj, err := renderValueMetricsErrorsRecentBuffer(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "recent_buffer", "Number of most recent diagnostics surfaced: the recent[] list in /metrics/errors and the recent_error series in the full /metrics/internal exposition. This caps size and cardinality. Valid range: 1..1000.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONMetricsPushObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("metrics.push.enabled") {
			valueObj, err := renderValueMetricsPushEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "enabled", "Enables periodic push to one VictoriaMetrics endpoint.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.push.url") {
			valueObj, err := renderValueMetricsPushUrl(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "url", "Base VictoriaMetrics URL, for example `http://127.0.0.1:8428`. Runtime appends `/api/v1/import/prometheus`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.push.interval") {
			valueObj, err := renderValueMetricsPushInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "interval", "Metrics push interval. Independent from metrics.snapshot_interval. Accepts values such as `15s`, `30s`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("metrics.push.timeout") {
			valueObj, err := renderValueMetricsPushTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "timeout", "Timeout for one metrics push request. Accepts values such as `10s`, `5s`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRateLimitObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRateLimitWebObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "web", "Rate-limit buckets for web listeners.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRateLimitYggObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "ygg", "Global and per-peer buckets for Yggdrasil HTTP traffic. Peer identity is the stable Yggdrasil IPv6 client address.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRateLimitWebObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRateLimitWebHttpObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "http", "Global and per-peer buckets for HTTP traffic.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRateLimitWebHttpsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "https", "Global and per-peer buckets for HTTPS traffic. Same token-bucket semantics as rate_limit.web.http.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRateLimitWebHttpObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.requests_per_second") {
			valueObj, err := renderValueRateLimitWebHttpRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "requests_per_second", "Sustained token refill rate for the global HTTP bucket. 0 disables the global HTTP limit. Example active value: 20.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.burst") {
			valueObj, err := renderValueRateLimitWebHttpBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "burst", "Global HTTP burst capacity. Required to be >= 1 when rate_limit.web.http.requests_per_second > 0. Example: 60.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRateLimitWebHttpPerPeerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "per_peer", "Per-peer HTTP bucket layered on top of the global HTTP bucket.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRateLimitWebHttpPerPeerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.per_peer.requests_per_second") {
			valueObj, err := renderValueRateLimitWebHttpPerPeerRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "requests_per_second", "Sustained per-peer HTTP rate. 0 disables per-peer HTTP limiting.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.per_peer.burst") {
			valueObj, err := renderValueRateLimitWebHttpPerPeerBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "burst", "Per-peer HTTP burst capacity. Required to be >= 1 when per_peer.requests_per_second > 0.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.http.per_peer.max_tracked") {
			valueObj, err := renderValueRateLimitWebHttpPerPeerMaxTracked(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_tracked", "Maximum concurrently tracked HTTP peers in the bounded LRU map. Required to be >= 1 when per-peer HTTP limiting is enabled.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRateLimitWebHttpsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.requests_per_second") {
			valueObj, err := renderValueRateLimitWebHttpsRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "requests_per_second", "Sustained token refill rate for the global HTTPS bucket. Same semantics as rate_limit.web.http.requests_per_second.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.burst") {
			valueObj, err := renderValueRateLimitWebHttpsBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "burst", "Global HTTPS burst capacity. Same validation rule as rate_limit.web.http.burst.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRateLimitWebHttpsPerPeerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "per_peer", "Per-peer HTTPS bucket layered on top of the global HTTPS bucket. Same model as rate_limit.web.http.per_peer.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRateLimitWebHttpsPerPeerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.per_peer.requests_per_second") {
			valueObj, err := renderValueRateLimitWebHttpsPerPeerRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "requests_per_second", "Sustained per-peer HTTPS rate. Same semantics as rate_limit.web.http.per_peer.requests_per_second.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.per_peer.burst") {
			valueObj, err := renderValueRateLimitWebHttpsPerPeerBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "burst", "Per-peer HTTPS burst capacity. Same validation rule as rate_limit.web.http.per_peer.burst.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.web.https.per_peer.max_tracked") {
			valueObj, err := renderValueRateLimitWebHttpsPerPeerMaxTracked(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_tracked", "Maximum concurrently tracked HTTPS peers. Same validation rule as rate_limit.web.http.per_peer.max_tracked.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRateLimitYggObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.requests_per_second") {
			valueObj, err := renderValueRateLimitYggRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "requests_per_second", "Sustained token refill rate for the global Yggdrasil bucket. Same semantics as rate_limit.web.http.requests_per_second.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.burst") {
			valueObj, err := renderValueRateLimitYggBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "burst", "Global Yggdrasil burst capacity. Same validation rule as rate_limit.web.http.burst.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRateLimitYggPerPeerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "per_peer", "Per-peer Yggdrasil bucket layered on top of the global Yggdrasil bucket. Same model as rate_limit.web.http.per_peer.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRateLimitYggPerPeerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.per_peer.requests_per_second") {
			valueObj, err := renderValueRateLimitYggPerPeerRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "requests_per_second", "Sustained per-peer Yggdrasil rate. Same semantics as rate_limit.web.http.per_peer.requests_per_second.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.per_peer.burst") {
			valueObj, err := renderValueRateLimitYggPerPeerBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "burst", "Per-peer Yggdrasil burst capacity. Same validation rule as rate_limit.web.http.per_peer.burst.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rate_limit.ygg.per_peer.max_tracked") {
			valueObj, err := renderValueRateLimitYggPerPeerMaxTracked(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_tracked", "Maximum concurrently tracked Yggdrasil peers. Same validation rule as rate_limit.web.http.per_peer.max_tracked.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONSourceObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("source.request_timeout") {
			valueObj, err := renderValueSourceRequestTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "request_timeout", "Timeout budget for one outgoing upstream operation: HTTP, git-provider API, or Brother RPC. Accepts values such as `30s`, `1m`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.download_max_parallel") {
			valueObj, err := renderValueSourceDownloadMaxParallel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "download_max_parallel", "Maximum number of source archive downloads running concurrently.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.download_max_duration") {
			valueObj, err := renderValueSourceDownloadMaxDuration(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "download_max_duration", "Hard wall-clock ceiling for a single source archive download attempt, independent of source.request_timeout (per-read idle) and the internal minimum-throughput floor. It bounds how long one slow upstream can hold a download slot (source.download_max_parallel).\n0 disables the ceiling. Set it above the time a legitimate largest-archive download needs: storage.archive_limits.size.compressed at your slowest expected link speed. Accepts values such as `1h`, `30m`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONSourceRateLimitObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "rate_limit", "Per-host token bucket for every outgoing upstream HTTP request: provider API listings, git refs advertisements, discovery probes, public mirror JSON, and archive downloads.\nThe bucket key is the lowercased target host. Brother RPC has its own limiter in brother.rpc.rate_per_sec.\nThe wait happens inside the HTTP transport, so it also paces retries and provider API pagination.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONSourceRetryObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "retry", "Retry policy for network and download failures. Runtime caps attempts in source code; these values shape the backoff between attempts.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONSourceCredentialsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "credentials", "Optional authorization for upstream git providers.\nCredentials raise public API limits and can unlock private sources. For example, GitHub anonymous access is usually much lower than PAT access.\nHeaders are applied only to matching provider API requests. They are never sent through mesh, Yggdrasil, or Brother traffic.\nEmpty values mean anonymous access.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONSourceRateLimitObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("source.rate_limit.requests_per_second") {
			valueObj, err := renderValueSourceRateLimitRequestsPerSecond(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "requests_per_second", "Sustained per-host request rate. 0 disables outbound rate limiting.\nKeep burst >= source.download_max_parallel + rescan.max_parallel_keys so parallel work does not starve against source.request_timeout.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.rate_limit.burst") {
			valueObj, err := renderValueSourceRateLimitBurst(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "burst", "Per-host burst capacity. Required to be >= 1 when requests_per_second > 0.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONSourceRetryObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("source.retry.backoff_initial") {
			valueObj, err := renderValueSourceRetryBackoffInitial(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "backoff_initial", "Initial retry delay. Accepts values such as `500ms`, `1s`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.retry.backoff_max") {
			valueObj, err := renderValueSourceRetryBackoffMax(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "backoff_max", "Upper bound for exponential retry delay. Must be >= source.retry.backoff_initial.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.retry.jitter_percent") {
			valueObj, err := renderValueSourceRetryJitterPercent(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "jitter_percent", "Retry jitter amplitude in percent. Valid range: 0..100.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONSourceCredentialsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("source.credentials.github") {
			valueObj, err := renderValueSourceCredentialsGithub(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "github", "GitHub PAT. Runtime sends `Authorization: Bearer <token>` to github.com and *.github.com, including api.github.com. Empty value keeps anonymous access.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.credentials.gitlab") {
			valueObj, err := renderValueSourceCredentialsGitlab(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "gitlab", "GitLab PAT. Runtime sends `PRIVATE-TOKEN: <token>` to gitlab.com and *.gitlab.com. Empty value keeps anonymous access.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.credentials.bitbucket") {
			valueObj, err := renderValueSourceCredentialsBitbucket(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "bitbucket", "Bitbucket access token. Runtime sends `Authorization: Bearer <token>` to bitbucket.org and *.bitbucket.org. Empty value keeps anonymous access.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("source.credentials.others") {
			valueObj, err := renderValueSourceCredentialsOthers(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "others", "Self-hosted or other providers such as Gitea, Gogs, or self-hosted GitLab. Map exact host -> full header value, for example `Authorization: token <t>` or `PRIVATE-TOKEN: <t>`. Exact host entries take precedence over built-in provider matching.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRescanObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("rescan.interval") {
			valueObj, err := renderValueRescanInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "interval", "Delay between rescan cycles. Accepts values such as `6h`, `15m`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rescan.max_parallel_keys") {
			valueObj, err := renderValueRescanMaxParallelKeys(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_parallel_keys", "Maximum number of registry keys scanned concurrently.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rescan.initial_depth") {
			valueObj, err := renderValueRescanInitialDepth(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "initial_depth", "Maximum source releases requested per scan. 0 means full available history.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONRescanVerifyObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "verify", "Tiered deep re-verification of already mirrored git versions. Between deep verifies a version is checked cheaply through the git refs advertisement (tag -> commit sha); the archive is re-downloaded only for new versions, sha drift, or when its verification tier is due. The latest version is deep-verified every cycle when its upstream SHA cannot be confirmed; with a confirmed matching SHA it follows recent_interval.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONRescanVerifyObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("rescan.verify.recent_interval") {
			valueObj, err := renderValueRescanVerifyRecentInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "recent_interval", "Deep-verify interval for recent versions (upstream positions 1..10 behind the latest). Accepts values such as `168h` or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("rescan.verify.archive_interval") {
			valueObj, err := renderValueRescanVerifyArchiveInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "archive_interval", "Deep-verify interval for archive versions (upstream positions 11 and older). Must be >= rescan.verify.recent_interval. Accepts values such as `720h` or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONStorageObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("storage.dir") {
			valueObj, err := renderValueStorageDir(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "dir", "Root filesystem directory for Pebble blobs, SQLite metadata, hot artifacts, and overlay state. Must not overlap logging.file.dir or web.static.dir.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.in_flight_read_bytes") {
			valueObj, err := renderValueStorageInFlightReadBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "in_flight_read_bytes", "Global budget for blob bytes materialized in RAM during read/write build paths. This is the main OOM backpressure for concurrent large artifact builds. Lower values reduce peak RAM but serialize more builds.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.overlay_build_max_parallel") {
			valueObj, err := renderValueStorageOverlayBuildMaxParallel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "overlay_build_max_parallel", "Maximum number of source-derived overlay materializations running concurrently.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONStoragePebbleObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "pebble", "Pebble durable engine tuning for blob and tree storage.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONStorageQuotaObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "quota", "Admission control and eviction for durable storage. The hot artifact cache has a separate storage.hot.max_size.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONStorageHotObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "hot", "Regenerable disk cache for built artifacts: Go/Composer zip files and universal zip/tar.gz files. This is separate from durable quota.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONStorageArchiveLimitsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "archive_limits", "Abuse-protection limits for source archives.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONStoragePebbleObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("storage.pebble.compression") {
			valueObj, err := renderValueStoragePebbleCompression(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "compression", "Compression profile for durable data blocks.\n`minlz`: default. On amd64 it usually stores slightly smaller than snappy with similar CPU; on arm64 it is effectively snappy. Pure Go, independent of CGO.\n`snappy`: slightly larger on disk than minlz, very low CPU, widely compatible. Pure Go.\n`fastest` / `fast`: same speed class as minlz/snappy with similar disk footprint. Pure Go.\n`zstd` / `good`: best disk density, but with CGO=0 the pure-Go codec is expensive on writes and raises allocation/GC pressure. Use it only when the build uses CGO=1 or disk is more important than write CPU.\n`balanced`: zstd only at the cold bottom of the LSM. Useful mainly on large stores with deep LSM levels; on small stores it behaves close to snappy.\n`none`: no compression. Highest disk use, lowest compression CPU. Useful for incompressible data or debugging.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.pebble.block_cache_size") {
			valueObj, err := renderValueStoragePebbleBlockCacheSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "block_cache_size", "Pebble block cache size. It stores hot decompressed blocks and mostly affects repeated durable reads. Size it near the hot working set; values far above the working set waste RAM.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.pebble.memtable_size") {
			valueObj, err := renderValueStoragePebbleMemtableSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "memtable_size", "Pebble memtable size and write-side RAM floor. Larger values reduce flush frequency, write CPU, and allocation churn on write-heavy ingest. It should fit large blobs; a practical target is >= storage.archive_limits.size.per_file.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.pebble.verify_on_read") {
			valueObj, err := renderValueStoragePebbleVerifyOnRead(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "verify_on_read", "Integrity check for durable blobs and trees when they are read to build artifacts.\n`always`: default. Re-hashes every durable blob with BLAKE3. Highest CPU cost on cache misses, strongest self-check against key/value mismatch.\n`sampled`: verifies one sampled read out of N. Lower CPU for high ingest or rebuild workloads while still catching corruption probabilistically.\n`never`: trusts Pebble block checksums on read. Fastest, but yggvault will not rehash durable values until a write or another verification path.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONStorageQuotaObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("storage.quota.max_total_size") {
			valueObj, err := renderValueStorageQuotaMaxTotalSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_total_size", "Hard cap for durable storage physical disk usage.\n0 disables durable quota checks and durable eviction.\nAccepts bytes or size strings such as `10gb` and `8GiB`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.quota.evict_to_size") {
			valueObj, err := renderValueStorageQuotaEvictToSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "evict_to_size", "Target durable size after an eviction cycle. Also works as the warning threshold.\nMust be > 0 and lower than storage.quota.max_total_size when durable quota is enabled.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.quota.retain_latest_per_key") {
			valueObj, err := renderValueStorageQuotaRetainLatestPerKey(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "retain_latest_per_key", "Minimum number of newest versions retained for every key. This is the lower bound during durable eviction.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.quota.max_versions_per_key") {
			valueObj, err := renderValueStorageQuotaMaxVersionsPerKey(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_versions_per_key", "Proactive hard cap on versions per key. 0 disables this per-key cap. Non-zero values must be >= storage.quota.retain_latest_per_key.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.quota.gc_interval") {
			valueObj, err := renderValueStorageQuotaGcInterval(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "gc_interval", "Interval for incremental durable GC. It removes orphan blobs left after crashes or interrupted writes. Accepts values such as `10m`, `1h`, or a nanosecond integer.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONStorageHotObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("storage.hot.max_size") {
			valueObj, err := renderValueStorageHotMaxSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_size", "Upper bound for the hot/ directory. Overflow triggers LRU and idle-first eviction. Larger values reduce rebuild storms; smaller values save disk but increase CPU by rebuilding artifacts.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.hot.idle_ttl") {
			valueObj, err := renderValueStorageHotIdleTtl(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "idle_ttl", "Idle TTL for built artifacts. Files not requested for longer than this can be deleted and rebuilt on the next request. Increase for read-heavy repeated downloads; decrease to save disk.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.hot.retain") {
			valueObj, err := renderValueStorageHotRetain(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "retain", "Hot-file retention policy: `all` keeps every built version, `latest` keeps only the newest version per key where possible, and `none` rebuilds on demand. Artifact hashes remain in the database.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.hot.verify_on_read") {
			valueObj, err := renderValueStorageHotVerifyOnRead(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "verify_on_read", "Integrity check for built hot files on the client-serving path.\n`always`: re-hashes the whole hot file on every serve. Strongest check, but latency is linear in artifact size and can dominate read-heavy large artifacts.\n`sampled`: default. Verifies one in 16 opens. Much cheaper than always while still detecting bit rot probabilistically.\n`never`: trusts file size and mtime on serve. Fastest, but a modified hot file is not detected until rebuild or another verification path.\nFor integrity-critical deployments use `always`; for read-heavy large artifacts use `sampled` or `never` after accepting the trade-off.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONStorageArchiveLimitsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONStorageArchiveLimitsSizeObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "size", "Byte caps for source archives.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONStorageArchiveLimitsEntriesObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "entries", "Structural caps for archive entries.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONStorageArchiveLimitsSizeObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.size.compressed") {
			valueObj, err := renderValueStorageArchiveLimitsSizeCompressed(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "compressed", "Maximum compressed source archive size. Accepts values such as `100mb`, `256MiB`, or bytes.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.size.unpacked") {
			valueObj, err := renderValueStorageArchiveLimitsSizeUnpacked(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "unpacked", "Maximum total unpacked bytes accepted from one source archive. Accepts values such as `512mb`, `1GiB`, or bytes.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.size.per_file") {
			valueObj, err := renderValueStorageArchiveLimitsSizePerFile(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "per_file", "Maximum single file size inside an archive. Must be <= storage.archive_limits.size.unpacked. Startup also rejects values above the real-memory safety cap. This value should fit in storage.pebble.memtable_size.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONStorageArchiveLimitsEntriesObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.entries.count") {
			valueObj, err := renderValueStorageArchiveLimitsEntriesCount(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "count", "Maximum number of entries accepted from one source archive.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("storage.archive_limits.entries.path_bytes") {
			valueObj, err := renderValueStorageArchiveLimitsEntriesPathBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "path_bytes", "Maximum byte length of one archive entry path.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONCacheObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("cache.metadata_max_size") {
			valueObj, err := renderValueCacheMetadataMaxSize(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "metadata_max_size", "RAM cap for prebuilt metadata and ETag cache. This is a hard size cap. Accepts values such as `64mb`, `256mb`, or bytes.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("cache.build_max_parallel") {
			valueObj, err := renderValueCacheBuildMaxParallel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "build_max_parallel", "Global cap on concurrent detached cache builds shared by the byte cache and the typed object cache. 0 disables the cap.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONOverlayObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONOverlayGoObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "go", "Global Go overlay safety settings.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONOverlayGoObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("overlay.go.rewrite_enabled") {
			valueObj, err := renderValueOverlayGoRewriteEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "rewrite_enabled", "Allows yggvault to rewrite go.mod and Go imports when the upstream module path differs from the public target module path.\nIf rewrite is required and this flag is false, the Go overlay for that version is not published.\nUniversal `.zip`/`.tar.gz` archives are always raw and unaffected by this flag.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONHistoryPolicyObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("history_policy.mutation") {
			valueObj, err := renderValueHistoryPolicyMutation(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "mutation", "`history` keeps historical pseudo-versions when an upstream version changes. `overwrite` replaces the stored version in place.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONHistoryPolicyDeletionObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "deletion", "Policy for upstream versions that disappear.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("history_policy.prefix") {
			valueObj, err := renderValueHistoryPolicyPrefix(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "prefix", "Prefix for generated historical pseudo-versions: `v0.0.0-<prefix><n>`. Must match `^[a-z][a-z0-9]{0,15}$`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("history_policy.max_events") {
			valueObj, err := renderValueHistoryPolicyMaxEvents(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_events", "Maximum number of stored history events used by Atom feeds and release history. Oldest events are deleted when the cap is exceeded.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONHistoryPolicyDeletionObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("history_policy.deletion.mode") {
			valueObj, err := renderValueHistoryPolicyDeletionMode(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "mode", "`keep` retains upstream-deleted versions locally. `delete` removes them after grace_cycles consecutive missing scans.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("history_policy.deletion.grace_cycles") {
			valueObj, err := renderValueHistoryPolicyDeletionGraceCycles(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "grace_cycles", "Number of consecutive cycles where a version must be missing upstream before deletion mode can delete it.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONUpstreamAvailabilityObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("upstream_availability.permanent_after_cycles") {
			valueObj, err := renderValueUpstreamAvailabilityPermanentAfterCycles(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "permanent_after_cycles", "Number of consecutive unavailable cycles before a source is classified as permanently unavailable.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONBrotherObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("brother.first_source_timeout") {
			valueObj, err := renderValueBrotherFirstSourceTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "first_source_timeout", "How long a Brother key waits for the original source during update checks before trying the brother fallback. The original source remains authoritative in first_source mode.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONBrotherRpcObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "rpc", "Limits for Brother RPC serving, outbound call pacing, and blob-fetch batch negotiation.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.prefer") {
			valueObj, err := renderValueBrotherPrefer(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "prefer", "`first_source` tries the original upstream first and uses Brother as fallback. `brother` tries Brother first and uses upstream as fallback, even after full Brother unavailability.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.transport_order") {
			valueObj, err := renderValueBrotherTransportOrder(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "transport_order", "Dial order when a Brother source advertises both web and Yggdrasil addresses: `web_ygg` tries web first, `ygg_web` tries Yggdrasil first.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONBrotherRpcObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("brother.rpc.web_enabled") {
			valueObj, err := renderValueBrotherRpcWebEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "web_enabled", "Expose Brother CONNECT /rpc on web listeners.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.ygg_enabled") {
			valueObj, err := renderValueBrotherRpcYggEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "ygg_enabled", "Expose Brother CONNECT /rpc on the Yggdrasil listener when Yggdrasil ingress is enabled.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.max_parallel") {
			valueObj, err := renderValueBrotherRpcMaxParallel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_parallel", "Maximum number of concurrent RPC sync sessions served on /rpc.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.max_parallel_per_peer") {
			valueObj, err := renderValueBrotherRpcMaxParallelPerPeer(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_parallel_per_peer", "Maximum concurrent RPC sync sessions from one remote address, layered under max_parallel so one client cannot occupy every slot. 0 auto-derives half of max_parallel; set >= max_parallel to effectively disable per-peer fairness.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.rate_per_sec") {
			valueObj, err := renderValueBrotherRpcRatePerSec(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "rate_per_sec", "RPC rate limit. The server applies it to new CONNECT /rpc sessions across web and Yggdrasil listeners; the source client applies it before outbound Brother RPC calls. 0 disables this limiter.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.max_fetch_response_bytes") {
			valueObj, err := renderValueBrotherRpcMaxFetchResponseBytes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_fetch_response_bytes", "Maximum total blob bytes returned by one Brother.BlobsFetch response. The server advertises this in Hello; clients batch by the lower local/remote value.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("brother.rpc.max_fetch_batch_count") {
			valueObj, err := renderValueBrotherRpcMaxFetchBatchCount(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_fetch_batch_count", "Maximum number of blob hashes accepted in one Brother.BlobsFetch request. The server advertises this in Hello; clients batch by the lower local/remote value.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONProfilingObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("profiling.enabled") {
			valueObj, err := renderValueProfilingEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "enabled", "Starts the dedicated pprof listener. Disabled by default.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("profiling.listen") {
			valueObj, err := renderValueProfilingListen(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "listen", "Loopback host:port for the pprof listener. Public and unspecified binds are rejected. Example remote access: `ssh -L 6060:127.0.0.1:6060 host`.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("profiling.max_profile_seconds") {
			valueObj, err := renderValueProfilingMaxProfileSeconds(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "max_profile_seconds", "Maximum accepted `?seconds=` value for /debug/pprof/profile and /debug/pprof/trace. Valid range: 1..600 seconds.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONProfilingContentionObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "contention", "Block and mutex contention profiling. Defaults are zero-overhead; enable only for targeted diagnostics.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONProfilingContentionObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("profiling.contention.block_rate") {
			valueObj, err := renderValueProfilingContentionBlockRate(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "block_rate", "runtime.SetBlockProfileRate value: one blocking event sampled per N nanoseconds. 0 disables block profiling.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("profiling.contention.mutex_fraction") {
			valueObj, err := renderValueProfilingContentionMutexFraction(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "mutex_fraction", "runtime.SetMutexProfileFraction value: one mutex contention event sampled per N events. 0 disables mutex profiling.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONEntryPrefix(bufferObj *bytes.Buffer, indentText string, keyText string, usageText string) {
	if usageText != "" {
		for _, lineText := range strings.Split(usageText, "\n") {
			bufferObj.WriteString(indentText + "  # " + lineText + "\n")
		}
	}
	keyArr, err := renderHJSONKeyText(keyText)
	if err != nil {
		keyArr = []byte(jsonQuoteForHJSON(keyText))
	}
	bufferObj.WriteString(indentText)
	bufferObj.WriteString("  ")
	bufferObj.Write(keyArr)
	bufferObj.WriteString(": ")
}

func renderHJSONValue(valueObj any, indentText string) ([]byte, error) {
	optsObj := hjson.DefaultOptions()
	optsObj.Comments = false
	// Значение-лист может быть объектом (map): ему нужны фигурные скобки, иначе пары ключ-значение
	// вылезают на строку с именем поля и ломают HJSON. Скобки самого корня пишет appendHJSONBranch.
	optsObj.EmitRootBraces = true
	optsObj.IndentBy = "  "
	optsObj.BaseIndentation = indentText
	dataArr, err := hjson.MarshalWithOptions(valueObj, optsObj)
	if err != nil {
		return nil, err
	}
	// Базовый отступ нужен для строк-продолжений, но первая строка идёт сразу после "key: ".
	return bytes.TrimLeft(dataArr, " "), nil
}

func renderHJSONKeyText(keyText string) ([]byte, error) {
	if isBareHJSONKey(keyText) {
		return []byte(keyText), nil
	}
	return json.Marshal(keyText)
}

func isBareHJSONKey(keyText string) bool {
	if keyText == "" {
		return false
	}
	for itemIndex, itemRune := range keyText {
		switch {
		case itemRune >= 'a' && itemRune <= 'z':
		case itemRune >= 'A' && itemRune <= 'Z':
		case itemRune >= '0' && itemRune <= '9' && itemIndex > 0:
		case itemRune == '_' || itemRune == '-':
		default:
			return false
		}
	}
	return true
}

func jsonQuoteForHJSON(keyText string) string {
	dataArr, err := json.Marshal(keyText)
	if err != nil {
		return "\"\""
	}
	return string(dataArr)
}
