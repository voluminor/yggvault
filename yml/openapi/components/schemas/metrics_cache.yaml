type: object
required: [ cache, storage ]
description: RAM metadata cache, hot-artifact cache, and storage-engine serving metrics.
properties:
  cache:
    type: object
    description: In-memory metadata and hot-artifact cache counters.
    required:
      - hits
      - misses
      - materializations
      - materializations_aborted
      - evictions
      - singleflight_shared
      - entries
      - bytes
    properties:
      hits: { type: integer, format: int64, example: 1824 }
      misses: { type: integer, format: int64, example: 37 }
      materializations: { type: integer, format: int64, example: 29 }
      materializations_aborted: { type: integer, format: int64, example: 1 }
      evictions: { type: integer, format: int64, example: 4 }
      singleflight_shared: { type: integer, format: int64, example: 12 }
      entries: { type: integer, format: int64, example: 96 }
      bytes: { type: integer, format: int64, example: 8388608 }
  storage:
    type: object
    description: Pebble engine gauges, event counters, and hot-cache enforcement counters.
    required:
      - live_bytes
      - obsolete_bytes
      - zombie_bytes
      - wal_bytes
      - memtable_bytes
      - compaction_debt_bytes
      - compaction_inprogress_bytes
      - compactions
      - flushes
      - block_cache_hits
      - block_cache_misses
      - write_stalls
      - background_errors
      - data_corruptions
      - disk_slow
      - possible_api_misuse
      - flush_duration_nanos
      - compaction_duration_nanos
      - hot_enforce_walks
      - hot_enforce_walk_duration_nanos
    properties:
      live_bytes: { type: integer, format: int64, example: 2147483648 }
      obsolete_bytes: { type: integer, format: int64, example: 67108864 }
      zombie_bytes: { type: integer, format: int64, example: 0 }
      wal_bytes: { type: integer, format: int64, example: 4194304 }
      memtable_bytes: { type: integer, format: int64, example: 33554432 }
      compaction_debt_bytes: { type: integer, format: int64, example: 16777216 }
      compaction_inprogress_bytes: { type: integer, format: int64, example: 0 }
      compactions: { type: integer, format: int64, example: 14 }
      flushes: { type: integer, format: int64, example: 31 }
      block_cache_hits: { type: integer, format: int64, example: 4096 }
      block_cache_misses: { type: integer, format: int64, example: 128 }
      write_stalls: { type: integer, format: int64, example: 0 }
      background_errors: { type: integer, format: int64, example: 0 }
      data_corruptions: { type: integer, format: int64, example: 0 }
      disk_slow: { type: integer, format: int64, example: 0 }
      possible_api_misuse: { type: integer, format: int64, example: 0 }
      flush_duration_nanos: { type: integer, format: int64, example: 824000000 }
      compaction_duration_nanos: { type: integer, format: int64, example: 3210000000 }
      hot_enforce_walks: { type: integer, format: int64, example: 24 }
      hot_enforce_walk_duration_nanos: { type: integer, format: int64, example: 142000000 }
